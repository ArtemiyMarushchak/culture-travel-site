# Montserrat (локально)

Единственный шрифт сайта. Файлы: `assets/fonts/montserrat/*.woff2`.

Подключение только через `fonts.local.css` → `styles/foundation/fonts.css` → `styles/main.css`.

Логотип и весь интерфейс рассчитаны на Montserrat. Внешняя подгрузка шрифтов не используется.
